# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so

# Packaging / builds
build/
dist/
*.egg-info/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.mypy_cache/

# Sphinx generated docs (source lives in source/)
docs/_build/

# --- IDEs ---
.idea/
.vscode/
*.swp

# --- OS ---
.DS_Store
Thumbs.db

# --- Large test fixtures (regenerate locally) ---
# 2D unconfined MF6 truth fixtures over 10 MB — rebuild with:
#   python working_tests/regenerate_unconfined_2d_truth.py
# The test auto-discovers whatever fixtures are present, so a fresh checkout
# runs the small committed grids and skips these until regenerated.
